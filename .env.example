# ─── Required ──────────────────────────────────────────────────────────────
# Base URL for the Sensybull backend API (no trailing slash)
# Example: https://api.sensybull.com or http://localhost:8000
API_BASE_URL=

# ─── Future / Optional ─────────────────────────────────────────────────────
# Sentry DSN for error tracking (leave blank to disable)
# SENTRY_DSN=

# NOTE: Do NOT add secrets or private keys here.
# The logo.dev token (currently in app/constants/theme.ts) should be moved
# to a backend proxy endpoint so it is never shipped in the app binary.
